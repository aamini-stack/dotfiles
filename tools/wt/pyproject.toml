[project]
name = "wt"
version = "0.1.0"
description = "Personal Jujutsu workflow helpers"
requires-python = ">=3.11"

[project.scripts]
wt = "wt.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
