[project]
name = "imgview"
version = "0.1.0"
description = "Inline image viewer via the Kitty graphics protocol"
requires-python = ">=3.11"

[project.scripts]
imgview = "imgview:main"

[tool.ruff]
target-version = "py311"

[build-system]
requires = ["uv_build>=0.9"]
build-backend = "uv_build"
