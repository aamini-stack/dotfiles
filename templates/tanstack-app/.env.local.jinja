# Bitwarden Secrets Manager machine account token. Prompted once on first
# `varlock load`, then encrypted in place. Machine-level — same token can be
# reused in every app.
# @sensitive
BWS_ACCESS_TOKEN=varlock(prompt)
