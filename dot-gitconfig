[rebase]
		updateRefs = true
[user]
	name = Aria Amini
	email = amini5454@gmail.com
[alias]
	append = town append
	compress = town compress
	contribute = town contribute
	diff-parent = town diff-parent
	down = town down
	hack = town hack
	delete = town delete
	observe = town observe
	park = town park
	prepend = town prepend
	propose = town propose
	rename = town rename
	repo = town repo
	set-parent = town set-parent
	ship = town ship
	sync = town sync
	up = town up
	lg = log --branches --remotes --tags --graph --oneline --decorate
	wt-add = !cd \"$(git rev-parse --git-common-dir)/..\" && git worktree add
